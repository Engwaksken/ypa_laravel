@extends('layouts.app')
@section('title', 'Order '.$order->order_number)
@section('content')
<div class="dash-wrap"><div class="dash-head"><div><div class="dash-greeting">Order Details</div><h1 class="dash-name">{{ $order->order_number }}</h1><div class="dash-date">{{ $order->created_at?->format('M d, Y H:i') }}</div></div><a href="{{ route('orders.index') }}" class="btn btn-outline-secondary"><i class="fas fa-arrow-left"></i> Back</a></div><div class="row g-4"><div class="col-lg-8"><div class="dash-panel"><div class="dash-panel-head"><span>Items</span><strong>UGX {{ number_format($order->total_amount, 0) }}</strong></div><div class="table-responsive"><table class="table"><thead><tr><th>Product</th><th>SKU</th><th>Qty</th><th class="text-end">Price</th><th class="text-end">Subtotal</th></tr></thead><tbody>@foreach($order->items as $item)<tr><td>{{ $item->product_name }}</td><td>{{ $item->sku ?: '-' }}</td><td>{{ $item->quantity }}</td><td class="text-end">UGX {{ number_format($item->price, 0) }}</td><td class="text-end">UGX {{ number_format($item->subtotal, 0) }}</td></tr>@endforeach</tbody></table></div></div></div><div class="col-lg-4"><div class="dash-panel"><div class="dash-panel-head">Customer</div><div class="dash-panel-body"><strong>{{ $order->customer_name }}</strong><p class="text-muted mb-1">{{ $order->phone }}</p><p class="text-muted mb-1">{{ $order->email ?: 'No email' }}</p><p class="mb-0">{{ $order->delivery_location }}</p></div></div><div class="dash-panel"><div class="dash-panel-head">Fulfillment</div><div class="dash-panel-body"><p><strong>Branch:</strong> {{ $order->branch->name ?? '-' }}</p><p><strong>Payment:</strong> {{ ucfirst(str_replace('-', ' ', $order->payment_method)) }}</p><p class="mb-0"><strong>Status:</strong> {{ ucfirst($order->status) }}</p></div></div></div></div></div>
@endsection
